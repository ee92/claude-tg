{
  "hooks": {
    "Stop": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "curl -fsS --max-time 3 -X POST -H 'Content-Type: application/json' --data-binary @- http://127.0.0.1:8765/stop >/dev/null 2>&1 || true",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
